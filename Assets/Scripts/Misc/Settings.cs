namespace Misc
{
    public static class Settings
    {
        #region Room Settings
        
        public const int maxChildCorridors = 3; // Max number of child corridors leading from a room -
                                                // max should be 3 although this is not recommended since
                                                // it can cause the dungeon building to fail since the rooms
                                                // are more likely to not fit together.
        
                                                
        #endregion Room Settings
    }
}
